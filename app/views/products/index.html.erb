<p id="notice"><%= notice %></p>
<% if !user_signed_in? %>
  <div class="jumbotron">
    <h1>CAPITALISIMO</h1>
    <p>Tu sitio para subir, comentar, recomendar y colocarle nota a tus peliculas favoritas. La más grande red de oponión de CINE, donde podras compartir y crear tu propia audiencia.</p>
    <p><%= link_to "Registrate aquí ", new_user_registration_path, class: "btn btn-primary btn-lg" %></p>
  </div>
<% end %>


<h1>PRODUCTOS</h1>



<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Category</th>
      <th>Picture</th>
      <th>Stock</th>
      <th>Price</th>
      <th>Start date</th>
      <th>End date</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.category %></td>
        <td><%= product.picture %></td>
        <td><%= product.stock %></td>
        <td><%= product.price %></td>
        <td><%= product.start_date %></td>
        <td><%= product.end_date %></td>
        <td><%= product.user_id %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path %>
